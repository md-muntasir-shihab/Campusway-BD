import AdminConsole from '@/components/AdminConsole';

export default function AdminDashboardPage() {
  return (
    <main className="container" style={{ padding: '1.2rem 0 2.4rem' }}>
      <AdminConsole />
    </main>
  );
}
